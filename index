<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Para Mi María Elena Mía — Felices 8 Meses</title>
    <meta name="description" content="Página dedicada a María Elena con música suave y recuerdos de nuestros 8 meses de noviazgo.">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>💗</text></svg>">
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&family=Lora:ital,wght@0,400;0,700;1,400&family=Playfair+Display:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <style>
        /* ===== Variables y ajustes generales ===== */
        :root {
            --primary-color: #e84393;
            --secondary-color: #ff7675;
            --bg-color: #fff0f5;
            --text-color: #2d3436;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-color);
            font-family: 'Lora', serif;
            overflow-x: hidden;
            scroll-behavior: smooth;
            background-image: radial-gradient(circle at 15% 20%, rgba(232,67,147,0.08) 0%, transparent 40%),
                              radial-gradient(circle at 85% 70%, rgba(255,118,117,0.08) 0%, transparent 40%);
        }

        .font-handwriting { font-family: 'Dancing Script', cursive; }
        .font-display { font-family: 'Playfair Display', serif; }

        /* ===== Animaciones de entrada ===== */
        .fade-in-up {
            opacity: 0;
            transform: translateY(20px);
            animation: fadeInUp 1.5s ease-out forwards;
        }
        @keyframes fadeInUp {
            to { opacity: 1; transform: translateY(0); }
        }
        .delay-100 { animation-delay: 0.1s; }
        .delay-300 { animation-delay: 0.3s; }
        .delay-500 { animation-delay: 0.5s; }
        .delay-700 { animation-delay: 0.7s; }

        /* ===== Corazón latiendo ===== */
        .heartbeat { animation: heartbeat 1.5s infinite; }
        @keyframes heartbeat {
            0% { transform: scale(1); }
            14% { transform: scale(1.3); }
            28% { transform: scale(1); }
            42% { transform: scale(1.3); }
            70% { transform: scale(1); }
        }

        /* ===== Tarjetas de recuerdos (polaroid) ===== */
        .polaroid {
            background: white;
            padding: 10px 10px 20px 10px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            position: relative;
            transform: rotate(calc(-3deg + 6deg * var(--rand)));
        }
        .polaroid:hover {
            transform: scale(1.05) rotate(0deg) !important;
            box-shadow: 0 10px 20px rgba(0,0,0,0.2);
            z-index: 10;
        }
        .polaroid img {
            width: 100%;
            height: 250px;
            object-fit: cover;
            border-radius: 4px;
        }

        /* ===== Corazones flotantes ===== */
        .floating-heart {
            position: fixed;
            color: var(--secondary-color);
            opacity: 0.6;
            pointer-events: none;
            z-index: 0;
            animation: floatUp 10s linear infinite;
        }
        @keyframes floatUp {
            0% { transform: translateY(100vh) scale(0) rotate(0deg); opacity: 0; }
            10% { opacity: 0.6; }
            90% { opacity: 0.6; }
            100% { transform: translateY(-10vh) scale(1.5) rotate(360deg); opacity: 0; }
        }

        /* ===== Barra de desplazamiento ===== */
        ::-webkit-scrollbar { width: 8px; }
        ::-webkit-scrollbar-track { background: var(--bg-color); }
        ::-webkit-scrollbar-thumb { background: var(--secondary-color); border-radius: 4px; }
        ::-webkit-scrollbar-thumb:hover { background: var(--primary-color); }

        /* ===== Carta de amor expansible ===== */
        #love-letter-container {
            transition: max-height 1s ease-in-out, opacity 0.5s ease-in-out;
            max-height: 0;
            opacity: 0;
            overflow: hidden;
        }
        #love-letter-container.open {
            max-height: 2000px;
            opacity: 1;
        }

        /* ===== Aparición de secciones al hacer scroll ===== */
        .reveal {
            opacity: 0;
            transform: translateY(24px);
            transition: opacity 0.8s ease, transform 0.8s ease;
        }
        .reveal.visible {
            opacity: 1;
            transform: translateY(0);
        }

        /* ===== Reproductor de música ===== */
        #music-player {
            position: fixed;
            bottom: 1rem;
            left: 50%;
            transform: translateX(-50%);
            z-index: 50;
            display: flex;
            align-items: center;
            gap: 0.75rem;
            background: rgba(255,255,255,0.88);
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
            border: 1px solid #ffd6e8;
            padding: 0.5rem 1rem;
            border-radius: 9999px;
            box-shadow: 0 6px 24px rgba(232,67,147,0.18);
            max-width: calc(100vw - 2rem);
        }
        #music-player button {
            background: none;
            border: none;
            color: #e84393;
            font-size: 1.25rem;
            cursor: pointer;
            transition: transform 0.2s ease;
            padding: 0.25rem;
        }
        #music-player button:hover { transform: scale(1.1); }
        .track-info {
            display: flex;
            flex-direction: column;
            justify-content: center;
            font-size: 0.8rem;
            line-height: 1;
            min-width: 0;
        }
        .track-title {
            font-weight: 600;
            color: #4a4a4a;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }
        .track-artist {
            color: #999;
            font-size: 0.7rem;
            margin-top: 2px;
        }
        #music-player input[type=range] {
            width: 80px;
            accent-color: #e84393;
        }
        @keyframes spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
        .spin { animation: spin 3s linear infinite; }
        @keyframes pulse-hint {
            0%, 100% { opacity: 0.9; transform: scale(1); }
            50% { opacity: 1; transform: scale(1.06); }
        }
        .hint-pulse { animation: pulse-hint 1.5s infinite; }
    </style>
</head>

<body class="relative min-h-screen">

    <!-- Corazones flotantes de fondo -->
    <div id="particles-container" class="fixed inset-0 pointer-events-none z-0"></div>

    <!-- Contenido principal -->
    <main class="relative z-10 container mx-auto px-4 py-8 md:px-8 max-w-4xl">

        <!-- Hero / Bienvenida -->
        <header class="min-h-[80vh] flex flex-col justify-center items-center text-center py-12">
            <div class="fade-in-up text-pink-500 text-6xl mb-4 heartbeat">
                <i class="fas fa-heart"></i>
            </div>
            <h1 class="font-handwriting text-5xl md:text-7xl lg:text-8xl text-pink-600 mb-6 fade-in-up delay-100 drop-shadow-sm">
                Felices 8 Meses,
            </h1>
            <h2 class="font-display text-4xl md:text-6xl font-bold text-gray-800 mb-8 fade-in-up delay-300">
                Mi María Elena Mía
            </h2>
            <p class="text-xl md:text-2xl text-gray-600 italic max-w-2xl fade-in-up delay-500">
                Ocho meses sumando sonrisas, compartiendo sueños y multiplicando el amor.
            </p>
            <p class="mt-6 text-sm text-pink-400 font-semibold fade-in-up delay-500">
                10 de enero de 2026 &nbsp;·&nbsp; 10 de septiembre de 2026
            </p>

            <a href="#nuestra-historia" class="mt-12 fade-in-up delay-700 bg-pink-500 hover:bg-pink-600 text-white rounded-full p-4 transition duration-300 ease-in-out shadow-lg hover:shadow-xl transform hover:-translate-y-1 animate-bounce cursor-pointer" aria-label="Bajar a nuestros momentos">
                <i class="fas fa-arrow-down text-2xl"></i>
            </a>
        </header>

        <!-- Contador -->
        <section id="contador" class="py-16 text-center border-t border-pink-200 reveal">
            <h3 class="font-display text-3xl md:text-4xl text-pink-600 mb-8 font-semibold">Cada segundo a tu lado cuenta</h3>
            <div class="grid grid-cols-2 md:grid-cols-4 gap-4 max-w-3xl mx-auto">
                <div class="bg-white p-6 rounded-2xl shadow-md border border-pink-100 transform hover:scale-105 transition">
                    <div id="months" class="text-4xl md:text-5xl font-bold text-pink-500 font-display">8</div>
                    <div class="text-gray-500 mt-2 uppercase tracking-wide text-sm font-semibold">Meses</div>
                </div>
                <div class="bg-white p-6 rounded-2xl shadow-md border border-pink-100 transform hover:scale-105 transition">
                    <div id="days" class="text-4xl md:text-5xl font-bold text-pink-500 font-display">243</div>
                    <div class="text-gray-500 mt-2 uppercase tracking-wide text-sm font-semibold">Días</div>
                </div>
                <div class="bg-white p-6 rounded-2xl shadow-md border border-pink-100 transform hover:scale-105 transition">
                    <div id="hours" class="text-4xl md:text-5xl font-bold text-pink-500 font-display">--</div>
                    <div class="text-gray-500 mt-2 uppercase tracking-wide text-sm font-semibold">Horas</div>
                </div>
                <div class="bg-white p-6 rounded-2xl shadow-md border border-pink-100 transform hover:scale-105 transition">
                    <div id="minutes" class="text-4xl md:text-5xl font-bold text-pink-500 font-display">--</div>
                    <div class="text-gray-500 mt-2 uppercase tracking-wide text-sm font-semibold">Minutos</div>
                </div>
            </div>
            <p class="mt-6 text-lg text-pink-500 font-semibold">+ <span id="seconds">00</span> segundos</p>
            <p class="mt-4 text-lg text-gray-600">Y siento que esto apenas es el comienzo de nuestra historia...</p>
        </section>

        <!-- Nuestros momentos -->
        <section id="nuestra-historia" class="py-16 border-t border-pink-200 reveal">
            <div class="text-center mb-12">
                <h3 class="font-handwriting text-5xl text-pink-600 mb-4">Nuestros Momentos</h3>
                <p class="text-gray-600 text-lg">8 instantes (uno por mes) que guardo en mi corazón.</p>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 px-4">
                <!-- Cambia los src de las imágenes por tus fotos reales -->

                <!-- Mes 1 -->
                <div class="polaroid" style="--rand: 0.8;">
                    <img src="https://raw.githubusercontent.com/michaelarc1/M-sica-para-dedicar-en-HTML-/main/IMG_20260110_222429.jpg" alt="Recuerdo Mes 1" loading="lazy" onerror="this.onerror=null; this.src='https://placehold.co/400x500/pink/white?text=Mes+1';">
                    <p class="text-center font-handwriting text-xl mt-4 text-gray-800">El comienzo de todo</p>
                </div>

                <!-- Mes 2 -->
                <div class="polaroid" style="--rand: 0.2;">
                    <img src="https://raw.githubusercontent.com/michaelarc1/M-sica-para-dedicar-en-HTML-/main/IMG_20260301_133109.jpg" alt="Recuerdo Mes 2" loading="lazy" onerror="this.onerror=null; this.src='https://placehold.co/400x500/pink/white?text=Mes+2';">
                    <p class="text-center font-handwriting text-xl mt-4 text-gray-800">Nuestra primera cita</p>
                </div>

                <!-- Mes 3 -->
                <div class="polaroid" style="--rand: 0.9;">
                    <img src="https://raw.githubusercontent.com/michaelarc1/M-sica-para-dedicar-en-HTML-/main/IMG_20260401_115952.jpg" alt="Recuerdo Mes 3" loading="lazy" onerror="this.onerror=null; this.src='https://placehold.co/400x500/pink/white?text=Mes+3';">
                    <p class="text-center font-handwriting text-xl mt-4 text-gray-800">Risas infinitas</p>
                </div>

                <!-- Mes 4 -->
                <div class="polaroid" style="--rand: 0.1;">
                    <img src="https://raw.githubusercontent.com/michaelarc1/M-sica-para-dedicar-en-HTML-/main/IMG_20260530_104605.jpg" alt="Recuerdo Mes 4" loading="lazy" onerror="this.onerror=null; this.src='https://placehold.co/400x500/pink/white?text=Mes+4';">
                    <p class="text-center font-handwriting text-xl mt-4 text-gray-800">Caminatas juntos</p>
                </div>

                <!-- Mes 5 -->
                <div class="polaroid" style="--rand: 0.7;">
                    <img src="https://raw.githubusercontent.com/michaelarc1/M-sica-para-dedicar-en-HTML-/main/IMG_20260602_084014.jpg" alt="Recuerdo Mes 5" loading="lazy" onerror="this.onerror=null; this.src='https://placehold.co/400x500/pink/white?text=Mes+5';">
                    <p class="text-center font-handwriting text-xl mt-4 text-gray-800">Complicidad</p>
                </div>

                <!-- Mes 6 -->
                <div class="polaroid" style="--rand: 0.3;">
                    <img src="https://raw.githubusercontent.com/michaelarc1/M-sica-para-dedicar-en-HTML-/main/IMG-20260714-WA0039.jpg" alt="Recuerdo Mes 6" loading="lazy" onerror="this.onerror=null; this.src='https://placehold.co/400x500/pink/white?text=Medio+A%C3%B1o';">
                    <p class="text-center font-handwriting text-xl mt-4 text-gray-800">Medio año mágico</p>
                </div>

                <!-- Mes 7 -->
                <div class="polaroid" style="--rand: 0.6;">
                    <img src="https://raw.githubusercontent.com/michaelarc1/M-sica-para-dedicar-en-HTML-/main/IMG_20260825_121926.jpg" alt="Recuerdo Mes 7" loading="lazy" onerror="this.onerror=null; this.src='https://placehold.co/400x500/pink/white?text=Mes+7';">
                    <p class="text-center font-handwriting text-xl mt-4 text-gray-800">Mirando al futuro</p>
                </div>

                <!-- Mes 8 -->
                <div class="polaroid" style="--rand: 0.4;">
                    <img src="https://raw.githubusercontent.com/michaelarc1/M-sica-para-dedicar-en-HTML-/main/1789070916766.png" alt="Recuerdo Mes 8" loading="lazy" onerror="this.onerror=null; this.src='https://placehold.co/400x500/pink/white?text=Mes+8';">
                    <p class="text-center font-handwriting text-xl mt-4 text-pink-600 font-bold">¡Hoy!</p>
                </div>
            </div>
        </section>

        <!-- 8 cosas que amo de ti -->
        <section class="py-16 border-t border-pink-200 reveal">
            <div class="text-center mb-10">
                <h3 class="font-display text-4xl text-gray-800 mb-2">8 Cosas que amo de ti</h3>
                <p class="text-pink-500 font-handwriting text-2xl">María Elena, eres especial porque...</p>
            </div>

            <div class="bg-white rounded-3xl p-8 md:p-12 shadow-xl border border-pink-100 relative">
                <div class="absolute top-0 right-0 -mt-6 -mr-6 text-5xl text-pink-300 opacity-50 transform rotate-12">
                    <i class="fas fa-quote-right"></i>
                </div>
                <ul class="space-y-4 text-lg md:text-xl text-gray-700">
                    <li class="flex items-start">
                        <span class="text-pink-500 mr-3 mt-1"><i class="fas fa-heart"></i></span>
                        <span>La forma en que tu sonrisa ilumina mis días más grises.</span>
                    </li>
                    <li class="flex items-start">
                        <span class="text-pink-500 mr-3 mt-1"><i class="fas fa-heart"></i></span>
                        <span>Tu inteligencia y cómo me inspiras a ser mejor persona.</span>
                    </li>
                    <li class="flex items-start">
                        <span class="text-pink-500 mr-3 mt-1"><i class="fas fa-heart"></i></span>
                        <span>La paciencia y ternura con la que me tratas siempre.</span>
                    </li>
                    <li class="flex items-start">
                        <span class="text-pink-500 mr-3 mt-1"><i class="fas fa-heart"></i></span>
                        <span>Nuestros abrazos que se sienten como un refugio seguro.</span>
                    </li>
                    <li class="flex items-start">
                        <span class="text-pink-500 mr-3 mt-1"><i class="fas fa-heart"></i></span>
                        <span>Tu risa contagiable que es mi melodía favorita.</span>
                    </li>
                    <li class="flex items-start">
                        <span class="text-pink-500 mr-3 mt-1"><i class="fas fa-heart"></i></span>
                        <span>La forma en que me miras, haciéndome sentir único en el mundo.</span>
                    </li>
                    <li class="flex items-start">
                        <span class="text-pink-500 mr-3 mt-1"><i class="fas fa-heart"></i></span>
                        <span>Todos los planes, grandes y pequeños, que imaginamos juntos.</span>
                    </li>
                    <li class="flex items-start">
                        <span class="text-pink-500 mr-3 mt-1"><i class="fas fa-heart"></i></span>
                        <span class="font-bold text-pink-600">Simplemente porque eres tú, mi María Elena mía, perfectamente imperfecta para mí.</span>
                    </li>
                </ul>
            </div>
        </section>

        <!-- Carta -->
        <section class="py-20 text-center reveal">
            <h3 class="font-display text-3xl text-gray-800 mb-8">Tengo un mensaje especial para ti...</h3>

            <button id="openLetterBtn" class="bg-gradient-to-r from-pink-400 to-pink-600 hover:from-pink-500 hover:to-pink-700 text-white font-bold py-4 px-10 rounded-full shadow-lg transform transition hover:scale-105 active:scale-95 text-xl flex items-center justify-center mx-auto space-x-3" aria-expanded="false">
                <i class="fas fa-envelope-open-text"></i>
                <span>Abrir mi carta</span>
            </button>

            <div id="love-letter-container" class="mt-8 bg-[#fffdf9] rounded-lg p-8 shadow-2xl mx-auto border-4 border-double border-pink-200 relative text-left">
                <div class="absolute top-0 right-0 p-4 opacity-20">
                    <i class="fas fa-stamp text-6xl text-pink-400 transform rotate-12"></i>
                </div>

                <h4 class="font-handwriting text-4xl text-pink-600 mb-6">Mi amor,</h4>

                <div class="font-serif text-lg leading-relaxed text-gray-700 space-y-4 relative z-10">
                    <p>
                        No puedo creer lo rápido que ha pasado el tiempo. Ya son 8 meses a tu lado, y cada día confirmo que coincidir contigo ha sido la mejor casualidad de mi vida.
                    </p>
                    <p>
                        Quería hacer algo diferente hoy para recordarte lo mucho que significas para mí. Eres mi paz, mi alegría, "Mi María Elena Mía". Me encanta cómo suena eso, porque aunque sé que eres una mujer libre, en mi corazón tienes un lugar que te pertenece solo a ti.
                    </p>
                    <p>
                        Gracias por estos ocho meses de aprendizajes, de apoyo mutuo, de besos robados y de noches de pláticas interminables. Prometo seguir cuidando de nosotros, seguir sacándote sonrisas y seguir enamorándote todos los días.
                    </p>
                    <p>
                        ¡Feliz aniversario de 8 meses, mi amor! Te amo muchísimo, hoy más que ayer, pero menos que mañana.
                    </p>
                </div>

                <div class="mt-10 text-right">
                    <p class="font-handwriting text-3xl text-pink-600">Con todo mi amor,</p>
                    <p class="font-handwriting text-2xl text-gray-800 mt-2">Michael ARC.</p>
                </div>
            </div>
        </section>

        <!-- Pie de página -->
        <footer class="py-6 text-center text-gray-500 text-sm mt-12">
            <p>Hecho con <i class="fas fa-heart text-pink-500 heartbeat inline-block"></i> exclusivamente para María Elena.</p>
            <p class="mt-1">Nicaragua, Septiembre 2026</p>
        </footer>

    </main>

    <!-- Canción dedicada: Que Te Quiero -->
    <audio id="music-audio" preload="metadata" loop src="https://raw.githubusercontent.com/michaelarc1/M-sica-para-dedicar-en-HTML-/main/Que%20Te%20Quiero%20%5BGI86H8OrJ8s%5D_cortado.mp3">
        Tu navegador no soporta audio HTML5.
    </audio>

    <!-- Reproductor de música -->
    <div id="music-player" role="group" aria-label="Reproductor de música">
        <button id="playPauseBtn" aria-label="Reproducir o pausar música">
            <i id="playIcon" class="fas fa-play"></i>
        </button>
        <div class="track-info">
            <span id="trackTitle" class="track-title">Que Te Quiero</span>
            <span id="trackArtist" class="track-artist">Nuestra canción</span>
        </div>
        <input type="range" id="volumeSlider" min="0" max="1" step="0.05" value="0.7" aria-label="Volumen">
        <button id="muteBtn" aria-label="Silenciar o reactivar sonido">
            <i id="muteIcon" class="fas fa-volume-up"></i>
        </button>
        <span id="hint" class="hint-pulse text-xs text-pink-500 ml-1">Toca ♡</span>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {

            // ===== 1. Corazones flotantes =====
            const particlesContainer = document.getElementById('particles-container');

            function createHeart() {
                const heart = document.createElement('i');
                heart.classList.add('fas', 'fa-heart', 'floating-heart');
                heart.style.left = `${Math.random() * 100}vw`;
                const size = Math.random() * 1.5 + 0.5;
                heart.style.fontSize = `${size}rem`;
                const duration = Math.random() * 5 + 8;
                heart.style.animationDuration = `${duration}s`;
                particlesContainer.appendChild(heart);
                setTimeout(() => heart.remove(), duration * 1000);
            }

            setInterval(createHeart, 800);
            for (let i = 0; i < 15; i++) {
                setTimeout(createHeart, Math.random() * 2000);
            }

            // ===== 2. Contador de tiempo juntos =====
            // ★ CAMBIA AQUÍ SI ES NECESARIO: la fecha exacta en que empezaron su noviazgo
            const FECHA_INICIO = new Date('2026-01-10T00:00:00');

            const daysEl = document.getElementById('days');
            const hoursEl = document.getElementById('hours');
            const minutesEl = document.getElementById('minutes');
            const secondsEl = document.getElementById('seconds');

            const formatNum = (n) => (n < 10 ? `0${n}` : `${n}`);

            function updateCounter() {
                const ahora = new Date();
                const diff = ahora.getTime() - FECHA_INICIO.getTime();

                if (diff <= 0) {
                    daysEl.textContent = '0';
                    hoursEl.textContent = '00';
                    minutesEl.textContent = '00';
                    secondsEl.textContent = '00';
                    return;
                }

                const diasTotal = Math.floor(diff / (1000 * 60 * 60 * 24));
                const restante = diff - diasTotal * (1000 * 60 * 60 * 24);
                const horas = Math.floor(restante / (1000 * 60 * 60));
                const minutos = Math.floor((restante % (1000 * 60 * 60)) / (1000 * 60));
                const segundos = Math.floor((restante % (1000 * 60)) / 1000);

                daysEl.textContent = diasTotal;
                hoursEl.textContent = formatNum(horas);
                minutesEl.textContent = formatNum(minutos);
                secondsEl.textContent = formatNum(segundos);
            }

            setInterval(updateCounter, 1000);
            updateCounter();

            // ===== 3. Carta de amor =====
            const btnCarta = document.getElementById('openLetterBtn');
            const cartaContenedor = document.getElementById('love-letter-container');
            let isLetterOpen = false;

            btnCarta.addEventListener('click', () => {
                isLetterOpen = !isLetterOpen;

                if (isLetterOpen) {
                    cartaContenedor.classList.add('open');
                    btnCarta.classList.add('shadow-2xl');
                    btnCarta.innerHTML = '<i class="fas fa-envelope"></i><span>Cerrar carta</span>';
                    btnCarta.classList.replace('from-pink-400', 'from-gray-400');
                    btnCarta.classList.replace('to-pink-600', 'to-gray-600');
                    btnCarta.setAttribute('aria-expanded', 'true');
                    setTimeout(() => {
                        cartaContenedor.scrollIntoView({ behavior: 'smooth', block: 'center' });
                    }, 300);
                } else {
                    cartaContenedor.classList.remove('open');
                    btnCarta.classList.remove('shadow-2xl');
                    btnCarta.innerHTML = '<i class="fas fa-envelope-open-text"></i><span>Abrir mi carta</span>';
                    btnCarta.classList.replace('from-gray-400', 'from-pink-400');
                    btnCarta.classList.replace('to-gray-600', 'to-pink-600');
                    btnCarta.setAttribute('aria-expanded', 'false');
                }
            });

            // ===== 4. Aparición de secciones al hacer scroll =====
            const revealObserver = new IntersectionObserver((entries) => {
                entries.forEach((entry) => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('visible');
                        revealObserver.unobserve(entry.target);
                    }
                });
            }, { threshold: 0.12 });

            document.querySelectorAll('.reveal').forEach((el) => revealObserver.observe(el));

            // ===== 5. Música: "Que Te Quiero" (MP3 de la página) =====
            const audioEl = document.getElementById('music-audio');
            audioEl.volume = parseFloat(document.getElementById('volumeSlider').value);

            const playPauseBtn = document.getElementById('playPauseBtn');
            const playIcon = document.getElementById('playIcon');
            const volumeSlider = document.getElementById('volumeSlider');
            const muteBtn = document.getElementById('muteBtn');
            const muteIcon = document.getElementById('muteIcon');
            const hint = document.getElementById('hint');
            const trackTitle = document.getElementById('trackTitle');
            const trackArtist = document.getElementById('trackArtist');

            trackTitle.textContent = 'Que Te Quiero';
            trackArtist.textContent = 'Nuestra canción';

            let isPlaying = false;
            let useGenerative = false;

            // --- Motor de respaldo: si el MP3 no puede cargarse, suena un piano suave ---
            let audioCtx = null;
            let masterGain = null;
            let padIndex = 0;
            let nextTime = 0;
            let schedulerTimer = null;

            const chords = [
                [261.63, 329.63, 392.00], // Do mayor
                [196.00, 246.94, 293.66], // Sol mayor
                [220.00, 261.63, 329.63], // La menor
                [174.61, 220.00, 261.63]  // Fa mayor
            ];

            function initGenerative() {
                if (audioCtx) return;

                audioCtx = new (window.AudioContext || window.webkitAudioContext)();
                masterGain = audioCtx.createGain();
                masterGain.gain.value = parseFloat(volumeSlider.value);

                const convolver = audioCtx.createConvolver();
                const impulseLength = Math.floor(audioCtx.sampleRate * 2.5);
                const impulse = audioCtx.createBuffer(2, impulseLength, audioCtx.sampleRate);
                for (let ch = 0; ch < 2; ch++) {
                    const data = impulse.getChannelData(ch);
                    for (let i = 0; i < impulseLength; i++) {
                        data[i] = (Math.random() * 2 - 1) * Math.pow(1 - i / impulseLength, 2.5);
                    }
                }
                convolver.buffer = impulse;

                const dry = audioCtx.createGain();
                dry.gain.value = 0.7;
                const wet = audioCtx.createGain();
                wet.gain.value = 0.35;

                masterGain.connect(dry).connect(audioCtx.destination);
                masterGain.connect(convolver).connect(wet).connect(audioCtx.destination);
                nextTime = audioCtx.currentTime;
            }

            function scheduleChord(time, freqs, dur = 4) {
                freqs.forEach((freq) => {
                    const osc = audioCtx.createOscillator();
                    osc.type = 'triangle';
                    osc.frequency.value = freq;

                    const gain = audioCtx.createGain();
                    gain.gain.setValueAtTime(0.0, time);
                    gain.gain.linearRampToValueAtTime(0.08, time + 0.9);
                    gain.gain.setValueAtTime(0.08, time + dur - 1.2);
                    gain.gain.linearRampToValueAtTime(0.0, time + dur);

                    const lp = audioCtx.createBiquadFilter();
                    lp.type = 'lowpass';
                    lp.frequency.value = 700;
                    lp.Q.value = 0.7;

                    osc.connect(lp).connect(gain).connect(masterGain);
                    osc.start(time);
                    osc.stop(time + dur + 0.05);
                });
            }

            function scheduleArpeggio(time, freqs, interval = 0.8, cycles = 4) {
                let t = time;
                for (let c = 0; c < cycles; c++) {
                    freqs.forEach((freq) => {
                        const osc = audioCtx.createOscillator();
                        osc.type = 'sine';
                        osc.frequency.value = freq * 2;

                        const gain = audioCtx.createGain();
                        gain.gain.setValueAtTime(0.0, t);
                        gain.gain.linearRampToValueAtTime(0.06, t + 0.05);
                        gain.gain.exponentialRampToValueAtTime(0.0001, t + 0.7);

                        const lp = audioCtx.createBiquadFilter();
                        lp.type = 'lowpass';
                        lp.frequency.value = 1200;

                        osc.connect(lp).connect(gain).connect(masterGain);
                        osc.start(t);
                        osc.stop(t + 0.75);
                        t += interval;
                    });
                }
            }

            function scheduleAhead() {
                while (nextTime < audioCtx.currentTime + 10) {
                    const chord = chords[padIndex % chords.length];
                    scheduleChord(nextTime, chord, 4);
                    scheduleArpeggio(nextTime, chord, 0.8, 4);
                    nextTime += 4;
                    padIndex++;
                }
            }

            function startScheduler() {
                if (schedulerTimer) return;
                schedulerTimer = setInterval(scheduleAhead, 1500);
            }

            function stopScheduler() {
                clearInterval(schedulerTimer);
                schedulerTimer = null;
            }

            function switchToGenerative() {
                useGenerative = true;
                trackTitle.textContent = 'Nuestra melodía';
                trackArtist.textContent = 'Piano suave (respaldo)';
                if (isPlaying) startGenerative();
            }

            function startGenerative() {
                initGenerative();
                if (audioCtx.state === 'suspended') audioCtx.resume();
                nextTime = audioCtx.currentTime;
                padIndex = 0;
                startScheduler();
            }

            function playMusic() {
                if (useGenerative) {
                    startGenerative();
                } else {
                    const promise = audioEl.play();
                    if (promise !== undefined) {
                        promise.catch((err) => {
                            // Si el error fue por falta de interacción del usuario (NotAllowedError),
                            // no descartamos el MP3, solo esperamos a que el usuario toque la pantalla.
                            if (err.name !== 'NotAllowedError') {
                                switchToGenerative();
                            }
                        });
                    }
                }

                isPlaying = true;
                playIcon.classList.remove('fa-play');
                playIcon.classList.add('fa-compact-disc', 'spin');
                hint.classList.add('hidden');
            }

            function pauseMusic() {
                if (!useGenerative) {
                    audioEl.pause();
                } else if (audioCtx) {
                    if (audioCtx.state === 'running') audioCtx.suspend();
                    stopScheduler();
                }

                isPlaying = false;
                playIcon.classList.remove('fa-compact-disc', 'spin');
                playIcon.classList.add('fa-play');
                hint.classList.remove('hidden');
            }

            playPauseBtn.addEventListener('click', () => {
                if (isPlaying) {
                    pauseMusic();
                } else {
                    playMusic();
                }
            });

            volumeSlider.addEventListener('input', () => {
                const val = parseFloat(volumeSlider.value);
                audioEl.volume = val;
                if (masterGain) masterGain.gain.setValueAtTime(val, audioCtx.currentTime);
                muteIcon.className = val === 0 ? 'fas fa-volume-mute' : 'fas fa-volume-up';
            });

            muteBtn.addEventListener('click', () => {
                if (parseFloat(volumeSlider.value) > 0) {
                    muteBtn.dataset.prevVol = volumeSlider.value;
                    volumeSlider.value = 0;
                    audioEl.volume = 0;
                    audioEl.muted = true;
                    if (masterGain) masterGain.gain.setValueAtTime(0, audioCtx.currentTime);
                    muteIcon.className = 'fas fa-volume-mute';
                } else {
                    const prev = muteBtn.dataset.prevVol || '0.7';
                    volumeSlider.value = prev;
                    audioEl.volume = parseFloat(prev);
                    audioEl.muted = false;
                    if (masterGain) masterGain.gain.setValueAtTime(parseFloat(prev), audioCtx.currentTime);
                    muteIcon.className = 'fas fa-volume-up';
                }
            });

            // Si el MP3 falla tras intentar descargarse, recién ahí activa el piano
            audioEl.addEventListener('error', (e) => {
                console.warn('Error cargando el MP3:', audioEl.error);
                switchToGenerative();
            });

            // Los navegadores piden un gesto del usuario para reproducir audio:
            // en el primer toque o clic en cualquier parte de la página, la música comienza.
            // Usamos 'click' y no 'pointerdown' para que, si el primer toque es el botón
            // del reproductor, su propio manejador no pause la canción justo después.
            function onFirstGesture() {
                if (!isPlaying) {
                    playMusic();
                }
                document.removeEventListener('click', onFirstGesture);
            }
            document.addEventListener('click', onFirstGesture, { once: true });
        });
    </script>
</body>
</html>
